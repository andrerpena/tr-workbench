import Button from './Button/Button';
import TextBox from './TextBox/TextBox';

var componentRegistry = { };
componentRegistry['button'] = Button;
componentRegistry['textbox'] = TextBox;

export default componentRegistry;

